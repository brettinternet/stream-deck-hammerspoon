# Hammerspoon examples

These examples register Stream Deck actions backed by Hammerspoon. Each file is a standalone starting point: copy it into your Hammerspoon configuration directory, or paste its contents into `~/.hammerspoon/init.lua` and adapt it.

## Use an example

1. Make the `streamdeck` Lua module available at `~/.hammerspoon/streamdeck` using the project setup instructions.
2. Keep the official Stream Deck application running. Use **Hammerspoon Toggle** when you want separate images for the example's returned inactive and active states; use **Hammerspoon Button** when one image is sufficient.
3. Select the example's action ID in that key's property inspector. If the example exposes settings, configure them there.
4. Reload Hammerspoon.

Every example calls `streamdeck.start()`. When combining examples in one `init.lua`, keep all registrations but leave only one `streamdeck.start()` call.

All examples require macOS, Hammerspoon, the official Stream Deck application, and the project's `streamdeck` Lua module. The suggested type reflects whether the example reports meaningful inactive and active states; a Button remains valid whenever you prefer one image.

Sound feedback in this release is Lua-only: there are no property-inspector sound settings. The shared `streamdeck.sound` module is configured in trusted Hammerspoon Lua; playback happens only after a successful callback. `lock-screen.lua` demonstrates a normal button-press cue, while `keep-awake.lua` demonstrates distinct post-action toggle cues by returning `sound.ON` or `sound.OFF`.

## Catalog

| File                                               | Action ID                                           | Suggested type | What it does                                                                                                                                                                                                                                         | Additional setup or dependencies                                                                                        |
| -------------------------------------------------- | --------------------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| [`app-launcher.lua`](app-launcher.lua)             | `com.brettinternet.hammerspoon.app-launcher`        | Toggle         | Launches an application if needed, then focuses it. The key is active while the configured app is frontmost.                                                                                                                                         | Uses `hs.application`. Set the app name and optional label in the action settings.                                      |
| [`application.lua`](application.lua)               | `com.brettinternet.hammerspoon.application-toggle`  | Toggle         | Opens a configured app when needed, reopens a running app whose main window was closed, hides/shows it on a tap, restores the previous frontmost app after hiding, and closes it on a long press. Without a bundle ID, it follows the frontmost app. | Uses `hs.application`. Set an application bundle ID and optionally enable focusing when showing in the action settings. |
| [`clipboard-clean.lua`](clipboard-clean.lua)       | `com.brettinternet.hammerspoon.clipboard-clean`     | Toggle         | Removes leading and trailing whitespace from the current text clipboard when pressed.                                                                                                                                                                | Uses `hs.pasteboard`. A text clipboard is required.                                                                     |
| [`clipboard-stash.lua`](clipboard-stash.lua)       | `com.brettinternet.hammerspoon.clipboard-stash`     | Toggle         | Stashes text on the first press and restores it on the next press for each key instance.                                                                                                                                                             | Uses `hs.pasteboard`. Stashes are in memory and require a text clipboard.                                               |
| [`focus-timer.lua`](focus-timer.lua)               | `com.brettinternet.hammerspoon.focus-timer`         | Toggle         | Starts or cancels a 25-minute focus timer per key; when it finishes, the key returns to Ready.                                                                                                                                                       | Uses `hs.timer`. No action settings.                                                                                    |
| [`keep-awake.lua`](keep-awake.lua)                 | `com.brettinternet.hammerspoon.keep-awake`          | Toggle         | Toggles display sleep prevention and reports Awake/Allow sleep through the toggle state; demonstrates distinct post-action sounds selected from the explicit resulting `sound.ON`/`sound.OFF` callback return.                                       | Uses `hs.caffeinate` and `streamdeck.sound`. Choose both state images in Stream Deck.                                   |
| [`keyboard-layout.lua`](keyboard-layout.lua)       | `com.brettinternet.hammerspoon.keyboard-layout`     | Toggle         | Switches between two keyboard layouts and highlights the key for the second layout.                                                                                                                                                                  | Uses `hs.keycodes`. Set the first and second layout names in the action settings; defaults are U.S. and Dvorak.         |
| [`last-application.lua`](last-application.lua)     | `com.brettinternet.hammerspoon.last-application`    | Toggle         | Switches to the previously active app; repeated presses toggle between the two most recent apps.                                                                                                                                                     | Uses `hs.application.watcher`. No action settings.                                                                      |
| [`lock-screen.lua`](lock-screen.lua)               | `com.brettinternet.hammerspoon.lock-screen`         | Button         | Locks the screen when pressed and demonstrates the shared normal button-press sound policy.                                                                                                                                                          | Uses `hs.caffeinate.lockScreen` and `streamdeck.sound`. No persistent active state or action settings.                  |
| [`meeting-mode.lua`](meeting-mode.lua)             | `com.brettinternet.hammerspoon.meeting-mode`        | Toggle         | Toggles meeting mode by muting the default microphone and preventing display sleep together.                                                                                                                                                         | Uses `hs.audiodevice` and `hs.caffeinate`. A default input device is required.                                          |
| [`microphone.lua`](microphone.lua)                 | `com.brettinternet.hammerspoon.microphone-toggle`   | Toggle         | Toggles the default microphone and shows Muted, Live, or No mic.                                                                                                                                                                                     | Uses `hs.audiodevice`. A default input device is required.                                                              |
| [`multi-instance.lua`](multi-instance.lua)         | `com.brettinternet.hammerspoon.per-instance-toggle` | Toggle         | Demonstrates independent toggle state on every key instance, including a configurable label.                                                                                                                                                         | Uses `streamdeck.helpers.perInstanceState` and `refreshAfter`. Set an optional label in the action settings.            |
| [`pomodoro.lua`](pomodoro.lua)                     | `com.brettinternet.hammerspoon.pomodoro`            | Toggle         | Runs four 25-minute focus cycles with 5-minute breaks and a final 15-minute break; pressing during a session resets it.                                                                                                                              | Uses `hs.timer`. Durations and cycle count are constants in the file.                                                   |
| [`url-launcher.lua`](url-launcher.lua)             | `com.brettinternet.hammerspoon.url-launcher`        | Button         | Opens a configured URL when pressed and displays the configured label.                                                                                                                                                                               | Uses `hs.urlevent`. Set a label and URL with a supported URL scheme in the action settings.                             |
| [`youtube.lua`](youtube.lua)                       | `com.brettinternet.hammerspoon.youtube`             | Button         | Plays or pauses the first YouTube video tab in Chromium, or opens a configured YouTube URL when no video tab exists.                                                                                                                                 | Uses Chromium's Hammerspoon JavaScript automation and `hs.eventtap`. Allow Hammerspoon to automate Chromium.            |
| [`window-center.lua`](window-center.lua)           | `com.brettinternet.hammerspoon.window-center`       | Toggle         | Moves the focused window to the center of its current screen without changing its size.                                                                                                                                                              | Uses `hs.window`. Hammerspoon needs macOS Accessibility permission.                                                     |
| [`window-maximize.lua`](window-maximize.lua)       | `com.brettinternet.hammerspoon.window-maximize`     | Toggle         | Toggles the focused window's Hammerspoon zoom state and tracks that state independently per key.                                                                                                                                                     | Uses `hs.window`. Hammerspoon needs macOS Accessibility permission.                                                     |
| [`window-next-screen.lua`](window-next-screen.lua) | `com.brettinternet.hammerspoon.window-next-screen`  | Button         | Moves the focused window to the next display while retaining its relative position and size.                                                                                                                                                         | Uses `hs.window`. Requires multiple displays and macOS Accessibility permission.                                        |
| [`window-snap.lua`](window-snap.lua)               | `com.brettinternet.hammerspoon.window-snap`         | Toggle         | Cycles the focused window through left half, right half, and full-screen layouts.                                                                                                                                                                    | Uses `hs.window`. Hammerspoon needs macOS Accessibility permission.                                                     |
